var fullName = "Pranay Gupta";
console.log(fullName);

/*how to name variables
1.  fullName
2.  full-name
3.  full_name   */

var courseName = `JavaScript Course by Hitesh Choudhary`; // this is a string
// strings can be enclosed in single or double quotes<""> or single quote<''> or single backtick<``>
// console.log(courseName);

var isLoggedIn = true; // this is a boolean variable it can store either <true> or <false> (these are case sensitive)
// console.log(isLoggedIn);

var loggedCount = 34; // this is a number
// console.log(loggedCount);

var paymentMode;
console.log(paymentMode); // this will print <undefined beacuse the variable paymentMode is not initialized

paymentMode = "Credit Card"; // this will assign "Credit Card" to the variable which was previously undefined
console.log(paymentMode); // this will print the value stored in the variable paymentMode which is "Credit Card"

// var if = 3; // the variable name if is one of the reserved keywords in JavaScript so we cannot name a variable with a reseverd keyword
